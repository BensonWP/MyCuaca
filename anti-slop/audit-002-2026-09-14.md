# Audit anti-slop 002, 2026-09-14

Level: Deep (P0-P6 + kontras terhitung).
Cakupan: 5 rute pasca redesain tema + toggle.
Dial: ENERGY 2 / RHYTHM 3 / MOTION 1, arah "langit reaktif" (tersimpan di DESIGN.md).

Semua temuan disetujui pengguna dalam satu putaran dan langsung diperbaiki.

## Temuan dan tindakan

[1] P2 A-14/I-3: semua permukaan berupa kartu putih ber-border seragam, hierarki rata.
    SELESAI: token latar `--surface` (zinc-50/zinc-900) menggantikan border pada KeyFacts, HourlyTimeline, AqiScale, MapView, CityManager, HomeScreen. Border kini hanya untuk kontrol, popover, dan alert.

[2] P3 D-8: footer mengulang navigasi header.
    SELESAI: footer jadi satu baris (nama + atribusi OpenWeather).

[3] P2 R-05: halaman terasa seragam meski dial RHYTHM 3.
    SELESAI: panel kota aktif jadi blok fokus sky-700 besar; angka AQI jadi 7xl/8xl sebagai focal point; hierarki tiap layar dibangun lewat bobot visual.

[4] P2 R-25: `text-white/85` di hero berpotensi gagal AA.
    SELESAI: semua teks hero warna solid; pasangan diverifikasi dengan contrast-check.py: sky-100/sky-700 5.17, slate-300/slate-950 13.59, slate-200/slate-700 8.40, blue-100/blue-900 8.49, stone-200/stone-700 8.18, slate-800/slate-200 8.40.

[5] P4 E-8: arah angin mentah "170 derajat" tidak terbaca.
    SELESAI: `windDirection()` 16 mata angin Bahasa Indonesia, dipakai KeyFacts.

[6] P3 A-10: `bg-white/95` dan `/98` tanpa fungsi.
    SELESAI: header dan nav bawah solid.

[7] P3 A-14: border ganda di nav bawah seluler.
    SELESAI: satu `border-t`.

[8] P6 J-2: `clock()` duplikat dua tempat.
    SELESAI: pindah ke `lib/format.ts`; ForecastScreen pakai `formatDay` yang sudah ada.

[9] P6 J-2: dead code `isFavorite`; tabrakan nama impor `removeFavorite`.
    SELESAI: fungsi dihapus; impor di-alias `removeFavoriteStored`.

[10] P3 R-19: MOTION 1 menjanjikan gerak pada perubahan status, implementasinya nol.
     SELESAI: kelas `.stateful` (transisi warna 150ms) di nav, tab tema/satuan/lapisan peta, selector hari, tombol aktif; override global `prefers-reduced-motion` di globals.css.

## Verifikasi ulang

`tsc` bersih, `eslint` bersih, `next build` sukses (9 rute), utilitas `bg-surface`/`stateful`/`--surface`/`prefers-reduced-motion` terkonfirmasi ada di CSS terbuild, smoke production 5 rute 200, API live, footer baru terkonfirmasi.
